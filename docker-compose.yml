version: '3.8'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
      - /path/to/host/fs:/data # Replace with actual host path for FS_PATH
    ports:
      - "8000:8000"
